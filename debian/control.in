Source: locationd
Section: utils
Priority: optional
Maintainer: Marius Gripsgard <marius@ubports.com>
Build-Depends: cmake,
               cmake-extras,
               curl,
               libdbus-cpp-dev (>= 4.1.0),
               debhelper (>= 9),
               doxygen,
               geoclue-ubuntu-geoip,
               google-mock (>= 1.6.0+svn437),
               graphviz,
               libapparmor-dev,
               libboost-filesystem-dev,
               libboost-program-options-dev,
               libboost-system-dev,
               libdbus-1-dev,
               libdbus-cpp-dev,
               libglib2.0-dev,
               libgoogle-glog-dev,
               libgtest-dev,
               libiw-dev,
               libjson-c-dev,
               libnet-cpp-dev,
               libprocess-cpp-dev,
               libtrust-store-dev,
               libubuntu-platform-hardware-api-headers [!arm64 !ppc64el !powerpc],
               libubuntu-platform-hardware-api-dev [!arm64 !ppc64el !powerpc],
               libproperties-cpp-dev,
               lsb-release,
               trust-store-bin,
Standards-Version: 3.9.4
Homepage: https://github.com/ubports/locationd

Package: liblocationd@UBUNTU_LOCATION_SERVICE_SOVERSION@
Section: libs
Architecture: any
Multi-Arch: same
Pre-Depends: ${misc:Pre-Depends}
Depends: ${misc:Depends},
         ${shlibs:Depends},
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains the shared library needed by client applications.

Package: liblocationd-dev
Section: libdevel
Architecture: any
Multi-Arch: foreign
Recommends: locationd-doc,
Depends: liblocationd@UBUNTU_LOCATION_SERVICE_SOVERSION@ (= ${binary:Version}),
         libdbus-1-dev,
         libdbus-cpp-dev,
         libboost-dev,
         ${misc:Depends},
Suggests: locationd-doc,
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains header files required to develop clients talking to the ubuntu
 location service.

Package: liblocationd-dbg
Section: debug
Architecture: any
Multi-Arch: foreign
Depends: liblocationd@UBUNTU_LOCATION_SERVICE_SOVERSION@ (= ${binary:Version}),
         ${misc:Depends},
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains debug symbols.

Package: locationd-tests
Architecture: any
Depends: ${misc:Depends},
         ${shlibs:Depends},
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains all test executables

Package: locationd-bin
Architecture: any
Depends: liblocationd@UBUNTU_LOCATION_SERVICE_SOVERSION@ (= ${binary:Version}),
         ${misc:Depends},
         ${shlibs:Depends},
         trust-store-bin,
Breaks: locationd-examples (<< 0.0.2),
Replaces: locationd-examples (<< 0.0.2),
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains the service executable and man pages.

Package: locationd-examples
Architecture: any
Multi-Arch: same
Depends: liblocationd@UBUNTU_LOCATION_SERVICE_SOVERSION@ (= ${binary:Version}),
         ${misc:Depends},
         ${shlibs:Depends},
         locationd-doc,
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains example service and client.

Package: locationd-doc
Section: doc
Architecture: all
Multi-Arch: foreign
Depends: ${misc:Depends},
         ${shlibs:Depends},
Recommends: locationd-examples,
Description: location service aggregating position/velocity/heading
 updates and exporting them over dbus.
 .
 Contains documentation for service and client.
